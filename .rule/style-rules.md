# Style Rules

- Use the styling approach adopted by the project consistently.
- Prefer shared design tokens for color, spacing, typography, and sizing.
- Keep styles scoped, readable, and close to the component or feature they support.
- Avoid one-off values when a reusable token or component is appropriate.
